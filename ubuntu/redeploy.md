```
ps aux --sort=-%mem | head

kill #numPid

git pull origin main --rebase

./gradlew clean build -x test

nohup java -jar build/libs/blueprint-0.0.1-SNAPSHOT.war > app.log 2>&1 &

tail -f app.log
```

## 권한 설정
```
chmod -R +x ~/BE
```
